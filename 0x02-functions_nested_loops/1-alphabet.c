#include<stdio.h>
#include"main.h"

/**
 *main - print alphabet
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char ch ='a';

       for (ch = 'a'; ch <= 'z'; ; ch++)
       {
	       _putchar(ch);

       }

       return (0);
}
