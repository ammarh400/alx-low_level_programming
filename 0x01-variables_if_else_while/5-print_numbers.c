#include<stdio.h>

/**
 *main -print digit of base 10
 *
 *Return: Always 0 (success)
 */

int main(void)
{
	int num = 0;

	for (num = 0; num < 10 ; num++)
	{
		printf("%d", num);
	}
	printf("\n");

	return (0);
}
