#include<stdio.h>

/**
 *main -print digit of base 10
 *
 *Return: Always 0 (success)
 */

int main(void)
{
	unsigned int num = 48;

	for (num = 48; num < 58 ; num++)
	{
		putchar(num);
	}
	putchar('\n');

	return (0);
}
